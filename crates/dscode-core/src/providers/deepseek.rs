//! DeepSeek provider (native API). (Stub — OpenAiProvider covers DeepSeek via OpenAI-compat)
pub struct DeepSeekProvider;
impl DeepSeekProvider {
    pub fn new(_api_key: String, _model: String) -> Self { Self }
}
