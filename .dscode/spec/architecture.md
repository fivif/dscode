# Project Architecture

> 自动注入到 DS Code 每一次会话的系统提示。
> 帮助 Agent 理解"东西在哪里、为什么"。

## 高层结构

- TODO：项目分层（domain / service / api / infra）或模块拓扑。

## 关键边界

- TODO：哪些模块允许互相调用？哪些不允许？

## 数据流

- TODO：用户输入 → ... → 持久化 → 响应 的主路径描述。

## 已知约束

- TODO：性能、合规、外部依赖等不可逾越的约束。
